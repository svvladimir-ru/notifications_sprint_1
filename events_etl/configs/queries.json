{
  "read": {
    "base": "SELECT {fields} FROM {schema}.{name}",
    "filter": "WHERE {filter_filed} IS {field_value}"
  },
  "update": {
    "base": "UPDATE {schema}.{name} SET",
    "set": "{field} = {field_value}",
    "where": "WHERE id = {id}"
  }
}